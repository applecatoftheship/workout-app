-- UIブラッシュアップPhase1・低リスク改善#10（2026年9月18日）
-- 種目マスタの表記ゆれ重複是正：「バイセップスカール（マシン）」と
-- 「バイセップス・カール（マシン）」（区切り点の有無のみが異なる同一種目）。
--
-- 【調査方法・実行状況】Claude Codeから本番Supabaseへは直接接続していないため、
-- アプリのUI（種目選択ドロップダウン・進捗グラフの「自己ベスト（PR）」一覧）を
-- 通常のブラウジングとして確認した。SQL Editorでの直接クエリ（本番シークレット
-- 経由の診断アクセス）は実行していない。本ファイルは未実行のDRAFTであり、
-- 実行前に人間によるSQL Editorでの最終確認・実行が必要（プロジェクト既存ルール）。
--
-- 【確認できた事実】
--   - exercises.id = f49aebff-0ae3-49c6-8c98-8e92ba33c618
--     name = 'バイセップスカール（マシン）'（区切り点なし）
--     自己ベスト実績あり：42kg（2026-09-07、進捗グラフで確認した最新のPR）
--   - exercises.id = 3e106555-9999-4c0f-9774-5434d3156a09
--     name = 'バイセップス・カール（マシン）'（区切り点あり）
--     自己ベスト実績あり：41kg（2026-08-28、進捗グラフで確認したPR）
--   → 両方に実際のtraining_log_exercises経由の記録が存在するため、
--     2026年8月19〜20日の「シーテッドロー」/「シーテッドロウ」の事故
--     （CLAUDE.md記載）と同種の重複と判断できる。
--
-- 【判断が必要な点・実行前に確認すること】
--   本SQLは f49aebff...（区切り点なし、より新しいPRの日付）を正式名として残し、
--   3e106555...（区切り点あり）を論理削除する前提で書いているが、どちらを
--   正式名とするかはユーザーの入力表記の意図次第であり、Claude Codeの判断のみでは
--   確定できない（CLAUDE.md「推測で仕様を補完しない」原則）。逆にしたい場合は
--   下記2箇所のUUIDを入れ替えてから実行すること。
--
-- 【安全性】training_sets自体は変更しない（exercise_idの付け替えのみ）ため、
-- セットの重量・回数データは失われない。物理削除ではなく論理削除
-- （is_deleted = true）のため、誤りがあれば手動で復元可能。

BEGIN;

-- 1. 重複側（区切り点あり）を参照している既存のtraining_log_exercisesを
--    正式名（区切り点なし）側のIDへ付け替える。
UPDATE training_log_exercises
SET exercise_id = 'f49aebff-0ae3-49c6-8c98-8e92ba33c618'  -- バイセップスカール（マシン）＝正式名（案）
WHERE exercise_id = '3e106555-9999-4c0f-9774-5434d3156a09'; -- バイセップス・カール（マシン）＝重複側（案）

-- 2. 重複側の種目マスタ行を論理削除する（物理削除はしない）。
UPDATE exercises
SET is_deleted = true
WHERE id = '3e106555-9999-4c0f-9774-5434d3156a09';

COMMIT;

-- 実行後の確認クエリ（任意）：
-- select id, name, is_deleted from exercises where name ilike '%バイセップス%カール%';
-- select exercise_id, count(*) from training_log_exercises
--   where exercise_id in ('f49aebff-0ae3-49c6-8c98-8e92ba33c618', '3e106555-9999-4c0f-9774-5434d3156a09')
--   group by exercise_id;
